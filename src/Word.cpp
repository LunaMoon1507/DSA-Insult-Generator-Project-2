// Each node in RB tree and value in hash table will be a Word
// (an instance of this class)

#include <string>

struct Word {
    std::string word;
    std::string pos;
    std::string vibe;
    int severity;
};
